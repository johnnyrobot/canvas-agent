node_modules/
dist/
build/
*.tsbuildinfo

# env / secrets (Canvas token lives in the macOS Keychain, not here)
.env
.env.local

# local data (SQLite DB, exports, uploads)
data/

# os / tooling
.DS_Store
*.log

# parallel agent-team build artifacts (dropped into worktrees)
.agent-status
AGENT_BRIEF.md
AGENT_CONTRACT.md

# WAVE oracle (dev-only) scratch output
/oracle-out/

# local agent / assistant state and tooling (never committed — project policy)
.claude/
.codex/
CLAUDE.md
AGENTS.md

# graphify knowledge-graph output (regenerated by `graphify update`; not tracked)
graphify-out/

# product requirements doc (kept out of git per project policy)
/docs/PRD_Canvas_Course_Design_Accessibility_Assistant.md
